# Event Tracking Contract

## Purpose
Define required event naming, payload fields, and ownership for analytics events.

## Contract
- Event names: `snake_case`
- Required fields: `event_name`, `timestamp`, `source_system`, `user_id` (where permitted)
- Optional fields must be documented in consumer integration specs.

## Support
See `support.md` for escalation and response model.
